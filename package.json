{
  "name": "api-replay-harness",
  "version": "0.1.0",
  "description": "Record real HTTP traffic and replay it against a different endpoint, reporting only the diffs that matter.",
  "license": "MIT",
  "author": "S. Woi",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "record": "tsc && node dist/record.js",
    "replay": "tsc && node dist/replay.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
